{
  "name": "S.E.A-System",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format" : "prettier --write .",
    "server:dev": "pnpm --filter server dev",
    "server:build": "pnpm --filter server build",

    "web:dev": "pnpm --filter web dev",
    "web:build": "pnpm --filter web build",

    "mobile:run": "pnpm --filter mobile run",
    "mobile:test": "pnpm --filter mobile test",

    "lint": "pnpm -r lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.2",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.60.0",
    "@typescript-eslint/parser": "^8.60.0",
    "eslint": "^10.4.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
